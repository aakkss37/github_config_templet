# linting, formatting, testing
name: Build, deploy and test "dark" version before allowing PR merge

on:
  workflow_dispatch:
  # pull_request:

jobs:
  placeholder:
    runs-on: [self-hosted, X64, ubuntu, Standard]
    steps:
      - run: echo "CHANGE ME"

  # build & deploy
  # e2e test
  # test:
  #   name: Testing
  #   runs-on: [self-hosted, X64, ubuntu, Standard]
